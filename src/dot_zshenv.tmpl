#!/usr/bin/env zsh

# both interactive and non-interactive zsh shells

shell_scripts=(
  env_vars
  path_management
)

for file in "${shell_scripts[@]}"; do
  [ -r ~/.shell/${file}.sh ] && source ~/.shell/${file}.sh
done

unset file

# HOMEBREW
eval "$(/opt/homebrew/bin/brew shellenv)"
